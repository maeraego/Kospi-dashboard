# KOSPI/KOSDAQ 레짐 타이밍 대시보드 — 프로젝트 안내

이 문서는 이 프로젝트에서 작업할 때 반드시 먼저 읽어야 할 핵심 맥락입니다.
과거에 이미 검증·결정된 사항들이라, 이를 모르면 해결된 문제를 다시 헤매게 됩니다.

## 프로젝트 개요

- **목적**: 코스피/코스닥의 향후 12개월 수익을 매크로·밸류·변동성 신호로 예측하는 레짐 타이밍 대시보드
- **메인 파일**: `build_dashboard.py` — 데이터 로드 → 신호 생성 → 가중 → 점수 → 레짐 사다리 → 예측 → 다크테마 HTML 생성
- **저장소**: `maeraego/Kospi-dashboard` (public), 웹: https://maeraego.github.io/Kospi-dashboard/
- **파이썬**: `C:\python312\python.exe`, 작업 폴더 `C:\Users\user\minyong-agent\Github`
- **언어**: 사용자와 한국어로 대화

## 데이터 파이프라인 (중요한 제약)

- **KRX는 해외 IP를 차단**한다. 그래서 GitHub Actions(미국 서버)에서는 데이터 수집 불가.
  → 데이터 수집·push는 **로컬 PC(한국 IP)**에서만 가능. `push_data.bat`(update_all.py → git add/commit/pull/push)가 이 역할.
  → 클라우드는 받은 parquet으로 **화면만 빌드**. 수집 안 함.
- **필수 데이터 파일** (로컬에 있음):
  - `krx_monthly.parquet`: KOSPI/KOSDAQ 종가·시총·PER·PBR
  - `ecos_monthly.parquet`: 기준금리·국고채·환율·선행지수·수출·**M1·M2** (M2는 2003-10~, 십억원 단위)
  - `krx_daily.parquet`, `fred_monthly.parquet`, `fwd_per_monthly.parquet`, `fwd_ni_monthly.parquet`
  - `kofia_daily.parquet`: 신용융자(전체·유가·코스닥)·투자자예탁금·미수금·반대매매·RP (1998-06-18~, 원 단위)
- **검증 원칙**: 반드시 M2가 있는 실제 parquet으로 `build_dashboard.py`를 돌려 확인할 것.
  과거에 M2 없는 환경에서 테스트해 "됐다"고 했다가 실제 화면과 어긋난 실패가 여러 번 있었다.
- **collect_flow.py(투자자 수급)는 KRX 로그인 필요** — .env의 KRX_ID/KRX_PW. 실패해도 메인 대시보드엔 영향 없음(별도 flow_ic 프로젝트용).

## 모델 방법론 (analyze 함수)

목표: 향후 12개월 지수 로그수익 예측.
파이프라인: 원자료 → expanding z-score(과거만, 최소 36개월) → 경제방향을 IC 부호로 확정 → |IC|≥0.10 & ≥60obs 필터(REF_ONLY 제외) → Ridge 다변량(λ=10·30·100) → **IC×Ridge×상관벌점 가중 + 변동성 극단증폭** → 합성점수 → 백분위 → 레짐.

### 핵심 결정사항 (사용자의 시장 직관이 데이터로 검증된 것들)

1. **변동성 = 역발상 매수신호 ("공포에 사라")** ★가장 중요
   - 실현변동성(20일)·VIX급등이 높을수록 이후 12개월 수익이 좋다 (상관 +0.19, 고변동 국면 승률 73%, 극단 상위15% 이후 +10~14%).
   - **코스피는 VKOSPI를 쓴다. 실현변동성은 코스닥 전용 + 코스피 참고지표.**
     예전엔 코스피도 실현변동성을 '한국VIX'라는 이름으로 썼는데 VKOSPI로 오해를 샀다.
     둘은 다르다 — 실현변동성 = 코스피 일간 로그수익률 20일 표준편차×√252(이미 일어난 등락),
     VKOSPI = 코스피200 옵션 내재변동성(앞으로 30일 기대 등락).
   - **실측(2003~ 공통구간)**: 수준상관 0.890이지만 **실현변동성이 VKOSPI를 4일 후행**(최대상관 0.914).
     IC 0.196 vs 0.181, 상위10% 진입 후 1년 +22.2%/승률86% vs +21.4%/82%, 표본외 0.7358 vs 0.7342.
   - **★ 둘 다 신호로 넣지 마라**: 상관 0.890이라 같은 말을 하는데 극단증폭이 양쪽에 걸려
     공포를 두 번 센다. 실측으로 변동성 기여가 +0.734 = 종합점수 +0.781의 94%까지 부풀었다.
     상관벌점은 증폭 *이전* 기본가중에만 걸려서 이 중복을 못 막는다.
   - **교체 효과(2026-08-12)**: 실현변동성 z=+5.07(백분위 99.6) → VKOSPI z=+3.05(97.9).
     급락이 지나갔는데 실현변동성이 20일간 끌고 다니며 공포를 과대평가하고 있었다.
     코스피 점수 +0.63 → **+0.25**, 백분위 90% → 72%, '매우 유리' → '유리'로 이동.
     (사용자가 "VKOSPI 56.48인데 반영이 안 됐다"고 짚어 발견)
   - **수집**: `collect_vkospi.py` (2003-01-02~, 5,828 거래일). KRX가 2025년 말부터 MDC 통계를
     로그인 뒤로 옮겨 비인증 호출은 400 → `collect_krx.py`가 쓰는 **pykrx 인증 세션을 재사용**해
     MDC JSON을 직접 호출한다. bld=`dbms/MDC/STAT/standard/MDCSTAT01201`,
     지수 `indTpCd=1, idxIndCd=300, idxCd=1, idxCd2=300`. **조회구간 2년 초과 시 INVALIDPERIOD2** → 2년씩 분할.
     pykrx 자체는 VKOSPI 티커를 제공하지 않는다(지수 168개에 없음).
   - **방향을 항상 강세(+1)로 고정**한다. IC 부호 자동 뒤집기에서 제외.
     이유: 급락이 '진행 중'이면 단변량 IC가 일시적으로 음수가 돼, 정작 바닥에서 '팔아라'로 뒤집히는 치명적 오류가 생김(2026 급락장에서 확인).
   - **극단 증폭**: |z|>2일 때만 가중 = max(기본, 하한 0.03) × (1 + 1.0×(|z|−2)). 평상시엔 증폭 0이라 표본외 보존.

2. **경기선행지수 순환참조** (KOSPI 전용)
   - 경기선행종합지수에 코스피가 구성항목으로 들어가 실측상 코스피가 +1개월 앞섬(동행/후행).
   - 원본 IC 0.565 중 코스피 성분 뺀 순수 IC는 0.421 (26%가 순환참조).
   - **신호 값은 원본 사용**(과거 직교화가 극단에서 붕괴), **가중용 IC만 순수분(0.421)으로 낮춤**.
   - KOSDAQ은 경기선행 구성에 없으므로 이 보정 안 함.

3. **Forward PBR (선행 PBR)** (KOSPI 전용, IC 0.333)
   - 예상이익이 자본으로 쌓여 미래 PBR이 낮아진다는 통찰.
   - 순자산=시총/PBR, 예상순이익=시총/예상PER, 미래순자산=순자산+예상순이익×0.65(사내유보), 선행PBR=시총/미래순자산.

4. **가중 방법 = IC × Ridge ÷ (1+평균상관)** (표본외 0.61→0.71→0.73)
   - 순수 Ridge는 억제변수를 부풀림 → IC 곱해 억제. 상관벌점으로 클러스터(금리 3형제) 완화.
   - 한계: 평균상관은 '한 쌍만 강한 겹침'(기준금리YoY↔일드커브 0.61)을 희석해 못 잡음. 문서화됨.
   - **매크로>가치지표는 버그 아님**: 이 모델은 12개월 예측력만 봄. 매크로 평균 IC 0.30 > 가치 0.23. 데이터 존중(사용자 선택). 용어설명에 명시됨.

5. **신용융자/예탁금 (투자자 레버리지)** (KOSPI 전용, **2007년~만 사용**, IC 0.364, 가중 9%)
   - 빚(신용융자) ÷ 대기현금(고객예탁금). 금융투자협회 FreeSIS 일별(원자료는 1998-06~).
   - **★ 2007년 이전은 반드시 잘라라 (구조 단절)**: 2007년 신용공여 제도 급성장으로 규모가
     한 자릿수 배 뛰었다 — 신용융자 2006년 0.47조 → 2007년 4.40조(9.4배), 비율 0.043 → 0.339.
     이 구간을 섞으면 expanding 평균이 0.237로 끌려 내려가, **최근 5년 기준 1.7 백분위(최저)인
     현재를 z=+0.28 '불리'로 표시**하는 오류가 난다(사용자가 차트와 신호분해 불일치로 발견).
     예상PER이 레벨 z 대신 추세괴리를 쓰는 것과 같은 이유.
   - **절단 시점 실측 비교** (표본외 IC / 전환점 적중):
     전기간 0.708(2008·2024 바닥 놓침) · 2005~ 0.699 · 2008~ 0.705 · EWM반감기5년 0.723(2021 고점 놓침)
     · **2007~ 0.734 ★ 전환점 5/5** (2011·2018·2021 고점 불리, 2020·2024 바닥 유리).
   - **기존 신호와 안 겹치는 게 핵심**: 평균절대상관 0.183(최대 0.390 일드갭). 기존 14개는
     밸류·금리·환율·경기·변동성만 재고 레버리지 축이 없었다 → 상관벌점 거의 안 맞아 가중 5위.
   - **7일~1년 5개 구간 모두 유의한 유일한 자금신호**. 다른 자금지표는
     단기=모멘텀(+)/장기=역발상(−)으로 부호가 뒤집혀 못 쓴다.
   - 꼬리(2007~): 하위5% 진입 후 1년 +30.4%(승률 96%)·6개월 +26.6%(승률 100%),
     상위5% 1년 −13.3%(승률 14%). 1년 IC −0.458.
   - **차트 표시**: `DATA.ic[지표]['from']/['fromWhy']`를 넣으면 지표비교 차트가 절단선·사유를
     자동 표시하고, 유불리 중앙값도 절단 이후로만 계산한다(안 그러면 차트와 신호분해가 어긋난다).
   - **코스닥은 넣지 않는다**: 평균 +0.002로 노이즈, 2020년 분할에선 악화. 가중만 10.5%로
     크게 잡혀 다른 신호 몫을 뺏는다.
   - **T+1 공표**: 일별에서 `shift(1)` 후 월말 추출. 안 하면 월말 종가와 같은 날 자금을 써서 룩어헤드.
   - **한계**: 일간 자금 *변화*는 지수를 3일 **후행**한다(CCF peak_lag=−3, 유의).
     며칠 앞을 보는 지표가 아니라 '빚으로 얼마나 버티나'를 재는 체온계. 극단에서만 신호가 켜진다.
   - `신용잔고/코스피지수`는 쓰지 마라 — 분모가 지수라 신호가 희석돼 전 구간 |t|<2.

6. **계절성 = 핼러윈 효과 (오버레이 전용, 종합점수 미반영)**
   - **"5~10월 지옥 / 11~4월 천국"은 실측으로 사실**. 코스피 겨울−여름 +1.89%p/월(t=2.29, p=0.022),
     코스닥 +3.23%p/월(t=3.26, p=0.001). 겨울만 보유 시 연율 코스피 +18.9%·코스닥 +18.5%,
     여름만 보유는 −4.7%·−18.2%. 1995년 이후 지수 상승분이 사실상 전부 11~4월에서 나왔다.
   - **사용자 표현 "4~10월 천국"은 오류**. 4월 한 달만 좋고(코스피 +3.25%, t=2.06) 5~10월은 전부 나빠서
     4~10월을 통으로 묶으면 연율 코스피 +0.8%·코스닥 −13.0%. 올바른 짝은 11~4월 / 5~10월.
   - **강건성**: IMF·닷컴·금융위기·코로나를 전부 빼도 남고(p=0.045·0.003), 상하 10% 절사평균에서도 남는다.
     10개 하위기간 전부 부호가 양수로 한 번도 뒤집힌 적 없음.
   - **★ 종합점수에는 절대 넣지 마라 — 12개월 지평에서 정의상 상쇄된다.**
     12개월 창은 어느 달에 시작하든 12개 달을 전부 담는다. 실측 진입월별 12개월 수익 차이
     코스피 p=0.757 · 코스닥 p=0.929로 IC≈0. |IC|≥0.10 필터에서 어차피 탈락한다.
   - **대신 6개월 지평에서 살아난다** — 순수 겨울창(10월말→4월말) 코스피 +10.6% vs 순수 여름창 −0.7%
     (p=0.031), 코스닥 +10.0% vs −9.0%(p=0.003). Newey-West 보정 t는 코스피 1.85·코스닥 2.95.
   - **★ 종합점수와 사실상 직교** — 상관 코스피 +0.017 · 코스닥 +0.042. 기존 15개 신호(밸류·금리·환율·
     경기·변동성·레버리지)가 재지 않는 축이라 대체가 아니라 **가산**된다. 결합 백테스트(2000-12~,
     신호 1개월 지연, 왕복 0.3%): 코스닥 레짐+계절 AND = CAGR 11.2% / 샤프 0.93 / MDD −15.0%
     (매수후보유 0.3% / −0.01 / −66.8%). 코스피는 OR이 우세(CAGR 14.3%).
   - **켈리 계절조정 = 가산(%p), 비율(승수) 아님**. `조정 f* = 기본 f* + 축소 0.5 × 기울기 × (겨울비중−0.5)`.
     기울기는 겨울우세창/여름우세창 6개월 켈리 차이의 실측값(코스피 +2.92 · 코스닥 +4.76).
     **비율을 쓰면 안 된다** — 코스닥 6개월 켈리 전체값이 +0.15로 0 근처라 승수가 13.6배로 폭발한다.
     켈리 앵커는 상하한을 ±150%/−50%가 아니라 −300%~+800%로 넓게 잡고 구한다(좁게 자르면
     f_전체가 상한에 포화돼 계절 효과가 통째로 사라짐).
   - **한계**: 코스피는 최근 10년 p=0.25로 유의하지 않다(살아있는 쪽은 코스닥, p=0.007).
     직전 2025년 여름은 코스피 +60.7%·코스닥 +25.5%로 정반대였다. 그래서 조정 폭을 절반으로 축소.
   - **파일**: `season_core.py`(계산 코어, **scipy 금지** — 클라우드는 pandas·numpy·pyarrow만 설치),
     `season_dashboard.py`→`season_dashboard.html`(전용 대시보드, 웹은 docs/season.html),
     `check_season.py`(진단, `--full`이면 레짐 결합분석까지).
     미완월 처리 필수 — `last_complete_month()`로 진행 중인 당월을 잘라야 반쪽 수익률이 안 섞인다.

7. **제거된 노이즈 신호 (REF_ONLY, 가중 0, 차트만 유지)**
   - `시가총액/M2` (IC 높지만 PBR과 0.84 겹침, 기여 미미)
   - `M2/M1 비율` (IC 0.16, 예측력 약함)
   - `수출 YoY` (IC −0.17, 경제상식과 방향 반대)
   - `M2 증가율(YoY)` (IC 0.04, 무의미)
   - 이들 제거 시 코스닥 표본외 개선(0.559→0.583).

### 기대수익 사다리 (레짐 표) — 7단계, 드리프트 보정

- **NB=7** (과거 10이었다가 표본 부족으로 축소). 라벨: '점수 X~Y% · 유리/불리/중립', 대칭 배분(유리3·중립1·불리3).
- **부드러운 십분위(_smooth_bins)**: 표본 220여개를 딱딱하게 나누면 노이즈로 비단조 → 각 구간을 중심 ±20% 겹침 이동창으로 계산. IC 0.5로 강해 겹침 평균만으로 자연 단조.
- **드리프트 v4**: 각 구간 = CAGR + (구간raw − 구간평균들의평균). 전체평균을 장기추세에 맞추되 상대편차 보존.
  - v1(표본평균빼기)=음수보정 버그, v2(중립앵커)=극단뭉갬 → v3/v4로 해결.
- 승률은 드리프트 보정 안 함. 켈리 베팅은 원본 수익 사용.

### 예측 밴드 (D방식) — 현재가 기반

- **문제**: 과거 '유리 국면'은 대개 폭락 직후 바닥(IMF·금융위기·코로나)이라 그때 반등률(+40~200%)을 지금 고점에 적용하면 과대추정(코스피 9,900 등).
- **D방식**: 예측중심 = 현재가 × exp(CAGR + 국면프리미엄×0.5).
  - CAGR: **1980.1.4=100 기준점**의 장기 CAGR 약 9.5% (1995 시작은 6.5%로 과소).
  - 국면프리미엄 = (그 국면 과거수익 − 전체평균), **축소계수 0.5**로 절반만 반영.
  - 밴드폭: 로버스트 표준편차(MAD×1.4826). **30/50/70/90% 구간 사용자 선택**(기본 50%).

## 대시보드 UI (다크 터미널, 자체완결 HTML)

- **체크박스 재계산**: 각 신호 체크 해제 시 종합점수·레짐·백분위·기대수익·예측박스 실시간 갱신.
  - **bin은 점수 백분위×NB로 계산**(_bins와 일치). 과거 qedges(경계 2개)로 찾다가 7단계와 안 맞아 −1%로 튀는 버그 있었음 → 수정됨.
  - 예측박스는 `updateProjChart()` 하나로 텍스트+히스토그램 밴드+중앙선+라벨 전부 동기화.
- **지표 비교 차트**: 명목지표(환율·유가·수출·M2 등)는 유불리 경계를 **우상향 로그-선형 추세선**으로(통화량 팽창 반영). 나머지는 수평 중앙값선.
  - 단, 예측 가중(IC)은 단순 z 유지(코스피에서 추세제거 z보다 예측력 높음). 차트 시각화만 추세선.

## 작업 워크플로우

1. `build_dashboard.py` 수정 → 실제 parquet으로 `python build_dashboard.py` 실행 → 점수 확인
2. 생성된 `dashboard.html`의 `<script>` 블록들 `node --check`로 JS 문법 검증
3. `check_*.py` 진단 스크립트로 가중·IC·드리프트 등 확인
4. git add → commit → **pull --no-edit** → push (pull 빠지면 rejected 발생)

## 주요 파일

- `build_dashboard.py` — 메인 생성기
- `collect_krx/ecos/fred/fwd_per/flow/vkospi.py` — 데이터 수집
- `collect_kofia.py` — 금융투자협회 FreeSIS 신용잔고·예탁금 (API 역공학: `tmpV40/41` 없으면 값이 전부 null, 응답 32KB에서 잘려 기간 재귀분할)
- `season_core.py` — 계절성 계산 코어 (메인·전용 대시보드가 공유, scipy 미사용)
- `season_dashboard.py` / `season_dashboard.html` — 계절성 전용 대시보드 (웹: docs/season.html)
- `check_season.py` — 계절성 진단 (`--full`이면 레짐점수 결합분석까지)
- `funds_pipeline.py` / `fund_signal_dashboard.html` — 자금신호 전용 분석·대시보드 (메인과 별개)
- `check_credit_weight.py` — 신용융자/예탁금의 가중·표본외 기여 측정
- `update_all.py` — 전체 수집+빌드
- `check_weights.py` — IC vs 가중 비교 (가장 자주 씀)
- `check_vixdir.py`, `check_vol.py`, `check_lowweight.py`, `check_band.py`, `check_drift_now.py`, `check_leading.py` — 진단

## 협업 원칙

- 사용자는 한국 주식시장 직관이 매우 정확하다. 모델이 직관과 어긋나면 데이터로 검증하되, 직관을 존중하라(변동성=유리, 순환참조, Forward PBR, 드리프트 등 모두 사용자가 먼저 짚었고 데이터가 확인함).
- 추측하지 말고 실제 데이터로 확인하라. 필요한 데이터가 없으면 사용자에게 요청하라.
- 면책: 과거 통계 기반 레짐 진단이지 투자권유 아님. 드리프트는 "미래에도 과거만큼 오른다" 가정.
