# 절대 올리면 안 되는 것 (API 키·비밀번호)
.env
*.env

# 파이썬 부산물
__pycache__/
*.pyc
.venv/
venv/

# 로컬 실행 흔적
update_log.txt
backup/
regime_state.json

# 로컬 대용량 (원하면 주석 해제해서 제외)
# *.parquet

# 리포트 산출물(매번 재생성)
dram_*.png
or_*.png
weekly_report_log.txt

# 공유용 산출물 (매일 재생성, 저장소에 쌓을 필요 없음)
share/

# proj_history.json 은 일부러 추적한다.
#   빌드마다 예측 중앙값 변화를 append 하는 파일인데, 무시하면
#   클라우드가 매번 빈 상태로 시작해 웹에서 이력이 영원히 1회분만 보인다.
